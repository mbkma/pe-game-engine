#ifndef GLOBAL_H
#define GLOBAL_H

// Represents the current state of the game
enum GameState {
    GAME_ACTIVE,
    GAME_MENU,
    GAME_WIN
};

// Represents the four possible directions
enum Direction {
    UP,
    RIGHT,
    DOWN,
    LEFT
};

#endif
